This is Perl module URI::Find::UTF8.

INSTALLATION

URI::Find::UTF8 installation is straightforward. If your CPAN shell is set up,
you should just be able to do

    % cpan URI::Find::UTF8

Download it, unpack it, then build it as per the usual:

    % perl Makefile.PL
    % make && make test

Then install it:

    % make install

DOCUMENTATION

URI::Find::UTF8 documentation is available as in POD. So you can do:

    % perldoc URI::Find::UTF8

to read the documentation online with your favorite pager.

Tatsuhiko Miyagawa
